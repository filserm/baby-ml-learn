version: "3.7"
services:
  baby-learn:
    build: 
      context: Dockerfile.base
    ports:
      - "4501:4501"
    container_name: "baby-learn"
    networks:
      - network-planner
  
  baby-monitor:
    build: 
      context: Dockerfile.code
    ports:
      - "4501:4501"
    container_name: "baby-monitor"
    networks:
      - network-planner
networks:
    network-planner:
        name: network-planner